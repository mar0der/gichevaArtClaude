1. secure the mysql with proper password and make sure phpmyadmin not accessible without password — done
2. replace the man image with Radka’s real image — done
3. seed and backup workflow for artworks database (data/artworks.sql, scripts/db_push.sh, scripts/db_backup.sh) — done
4. optimize the static images — pending
